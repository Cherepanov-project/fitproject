import { IColors } from "../models.interface"

const blue = "#3751FF"
const gray = "#9FA2B4"
const black = "#252733"

export const defaultColors: IColors = {
    nameColor: gray,
    valueColor: black,
    borderColor: gray,
}
export const hoverColors: IColors = {
    nameColor: blue,
    valueColor: blue,
    borderColor: blue,
}
